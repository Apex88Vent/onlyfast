Add optional track logo images here using the track name as a lowercase slug.

Examples:
- Barona Speedway -> barona-speedway.png
- Ventura Speedway -> ventura-speedway.png

When the Home page has a matching current weekend track, it will show the image
in the Current Race Weekend card. If no matching image exists, the app hides the
image panel and keeps the card layout usable.
